{
    "configurations": [
        {
            "name": "Run DIFFSTUB Tests",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/test_ngx_diffstub_internal",
            "cwd": "${workspaceFolder}"
        }
    ],
    "version": "0.2.0",
    "compounds": []
}
